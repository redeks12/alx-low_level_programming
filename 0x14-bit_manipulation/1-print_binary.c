#include <main.h>

void print_binary(unsigned long int n)
{
        unsigned long int binNum = 0;
        int power = 0;
        int rem;

        while (n > 0)
        {
                rem = n % 2;
        }
        
}
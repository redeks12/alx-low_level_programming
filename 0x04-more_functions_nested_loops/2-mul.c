#include "main.h"
/**
 * mul - a function that adds two integers and returns the result
 * @a: first number input
 * @b: second number input
 * Return: b + a
 */
int mul(int a, int b)
{
	return (a * b);
}
